#include <stdio.h>
#include "main.h"

/**
  *main - Entry point
  *@argc: number of arguments
  *@argv: pointer to arguments
  */

void main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
