#include <stdio.h>

/**
 * main - Entry point
 *
 * Description : 'This is code'
 *
 * Return: Always return 0(Success)
 */
int main(void)
{
char a = 'a';
char z = 'z';

while (a <= z)
{
putchar(a);
a++;
}
putchar(10);
return (0);
}
