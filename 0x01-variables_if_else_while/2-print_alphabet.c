#include <stdio.h>

/**
 * main - Entry point
 *
 * Description : 'This is code'
 *
 * Return: Always return 0(Success)
 */
int main(void)
{
char a = 'a';

while (a <= 122)
{
putchar(a);
a++;
}
putchar(10);
return (0);
}
